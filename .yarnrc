# Never run dependency lifecycle scripts on install. The only packages in
# this tree with install scripts are core-js (funding banner) and @swc/core
# (binding fallback; the real binary ships as an optional dependency), so
# nothing is lost — and a compromised dependency cannot execute code at
# install time. CI passes the equivalent --ignore-scripts flag explicitly.
ignore-scripts true
